import { MoedaBrasilPipe } from './moeda-brasil.pipe';

describe('MoedaBrasilPipe', () => {
  it('create an instance', () => {
    const pipe = new MoedaBrasilPipe();
    expect(pipe).toBeTruthy();
  });
});
