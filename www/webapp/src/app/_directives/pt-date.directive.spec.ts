import { PtDateDirective } from './pt-date.directive';

describe('PtDateDirective', () => {
  it('should create an instance', () => {
    const directive = new PtDateDirective();
    expect(directive).toBeTruthy();
  });
});
